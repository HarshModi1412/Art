fastapi>=0.110
uvicorn[standard]>=0.29
pandas>=2.0
python-multipart>=0.0.9
openai>=1.30
razorpay>=1.4
openpyxl>=3.1
xlrd>=2.0.1
numpy>=1.24
reportlab
matplotlib
Pillow
requests
cryptography>=41
supabase>=2.0,<3
huggingface_hub>=0.34
# Watermark remover (backend/core/watermark.py): OpenCV inpaints the corner
# mark, imageio-ffmpeg ships a static ffmpeg to re-encode cleaned clips as H.264.
opencv-python-headless>=4.9
imageio-ffmpeg>=0.5
# Optional: only imported when SENTRY_DSN is set. The built-in
# error log in backend/core/errors.py works without it.
# sentry-sdk>=2.0
